apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "testvolume"
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: "lvms-vg0"
  resources:
    requests:
      storage: "1Gi"
  volumeMode: Filesystem
